---
name: itk-stage-census
description: Use when the user asks how many, or which, ITk modules are currently at a given production stage right now — e.g. "How many and which modules are currently at the FINAL_METROLOGY stage at IRFU?", optionally split across institutions (including the "Paris" grouping of IRFU + LPNHE + IJCLab). This is a live snapshot of current stage, not a historical count of past sign-offs — for that, use itk-signoff-reports.
---

Follow the full recipe in `playbooks/STAGE_CENSUS.md` (repository root). That file is the canonical, tool-agnostic version of this playbook — keep it in sync if you edit either copy.
