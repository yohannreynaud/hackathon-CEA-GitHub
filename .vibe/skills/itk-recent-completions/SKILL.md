---
name: itk-recent-completions
description: Use when the user asks which NEW modules have completed/passed a given production stage since a recent date — e.g. "What are the new modules which have successfully completed the FINAL_COLD stage at IRFU since last Wednesday". Answers with a list of modules, not just a count — for a pure count use itk-production-rate.
---

Follow the full recipe in `playbooks/RECENT_COMPLETIONS.md` (repository root). That file is the canonical, tool-agnostic version of this playbook — keep it in sync if you edit either copy.
