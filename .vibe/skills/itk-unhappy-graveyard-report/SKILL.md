---
name: itk-unhappy-graveyard-report
description: Use for a database-wide survey of ALL modules currently in UNHAPPY or GRAVEYARD stage, together with their comments and the stage they were in right before — e.g. "I want a list of modules in UNHAPPY (respectively GRAVEYARD) stage, with the list of comments for each, and the stage before, as a table with columns AtlasId, Alias, CurrentStage, StageBeforeUnhappy, comments." For a single named component, use itk-component-investigation instead — it's a smaller, faster version of the same recipe.
---

Follow the full recipe in `playbooks/UNHAPPY_GRAVEYARD_REPORT.md` (repository root). That file is the canonical, tool-agnostic version of this playbook — keep it in sync if you edit either copy.
